# Environment Configuration
# Set to 'production' for production deployment, 'staging' for staging, etc.
ENVIRONMENT=production

# SSL Configuration
# Set to 'true' to enable SSL, 'false' to disable
USE_SSL=true

# Traefik Configuration
# Email address for Let's Encrypt notifications
ACME_EMAIL=your-email@example.com
# Domain for Traefik dashboard
TRAEFIK_DASHBOARD_DOMAIN=traefik.yourdomain.com
# Basic auth for Traefik dashboard. Generate using htpasswd
TRAEFIK_DASHBOARD_AUTH=admin:$apr1$hashedpassword
# Domain for your API
APP_DOMAIN=api.yourdomain.com
# Domain for your web application
WEB_DOMAIN=app.yourdomain.com

# Database Configuration
# PostgreSQL user, password, and database name
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=
POSTGRES_DB=your_database_name